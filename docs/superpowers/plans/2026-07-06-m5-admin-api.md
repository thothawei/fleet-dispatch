# M5-ADMIN：後台認證 + 唯讀 Admin API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 在 `line-fleet-dispatch` 加入後台管理員（admin）認證與一組唯讀 Admin API（即時車隊、司機列表、訂單列表、訂單軌跡、日報表），供 M8 後台前端（React+TS）取用；搭配已完成的 WS `driver.location` 廣播即可做即時車隊地圖。

**Architecture:** 新增 `admins` 表 + `Admin` model + `AdminRepository` + `AdminRegistry`（bcrypt，鏡射既有 `DriverRegistry`）。認證沿用已完成的 `auth.GenerateToken("admin", id, ...)` 與 `ParseToken`；新增 `middleware.AdminAuth` 驗 `role=admin`。Admin API 重用既有 repo（report/ride/track）與新增少量唯讀查詢，Redis 新增「列出所有在線司機位置」。管理員帳號由啟動時的環境變數種子建立（不開放公開註冊端點）。

**Tech Stack:** Go 1.25 / Gin / GORM / golang-jwt v5 / bcrypt / zerolog / go test（testcontainers 慣例）。

## Global Constraints

- Go module `line-fleet-dispatch`，Go 1.25.0。分層 `Handler → Service → Repository → DB/Redis` 不破壞。
- 註解/log/使用者訊息一律**繁體中文**；識別字英文。
- JWT 沿用既有 `internal/auth`（HS256、`cfg.JWTSecret`）；admin token 用 `auth.GenerateToken(events.RoleAdmin=="admin", adminID, secret, ttl)`。
- **不新增會被派單邏輯忽略的假欄位**：本 plan 只做唯讀 API 與認證，不加司機停用（留待後續）。
- migration 檔案編號**接續既有到 000004**，下一個是 `000005`（切勿重用既有編號，會害 golang-migrate 失敗）。
- 這台 Mac cgo 壞：所有 `go build`/`go test` 前綴 `CGO_ENABLED=0`（或用 `make test`）。整合測試需 Docker。
- 直接在 `main` 開發；每個 Task 結尾 `git commit`，全部完成後 `git push`（遠端 thothawei/fleet-dispatch）。
- TDD：先寫失敗測試 → 紅 → 最小實作 → 綠 → commit。

---

### Task 1：admins 表 migration 與 Admin model

**Files:**
- Create: `db/migrations/000005_create_admins.up.sql`
- Create: `db/migrations/000005_create_admins.down.sql`
- Modify: `internal/model/models.go`（新增 `Admin`）

**Interfaces:**
- Produces: `model.Admin{ ID int64; Email string; PasswordHash string; Name string; CreatedAt, UpdatedAt time.Time }`、`Admin.TableName() == "admins"`

- [ ] **Step 1：寫 up migration**

Create `db/migrations/000005_create_admins.up.sql`：
```sql
-- 後台管理員帳號（email + bcrypt 密碼）
CREATE TABLE IF NOT EXISTS admins (
    id            bigserial PRIMARY KEY,
    email         text UNIQUE NOT NULL,
    password_hash text NOT NULL DEFAULT '',
    name          text NOT NULL DEFAULT '',
    created_at    timestamptz NOT NULL DEFAULT now(),
    updated_at    timestamptz NOT NULL DEFAULT now()
);
```

- [ ] **Step 2：寫 down migration**

Create `db/migrations/000005_create_admins.down.sql`：
```sql
DROP TABLE IF EXISTS admins;
```

- [ ] **Step 3：新增 Admin model**

在 `internal/model/models.go` 末尾新增：
```go
type Admin struct {
	ID           int64     `gorm:"primaryKey"`
	Email        string    `gorm:"uniqueIndex;not null"`
	PasswordHash string    `gorm:"column:password_hash;not null;default:''"`
	Name         string    `gorm:"not null;default:''"`
	CreatedAt    time.Time `gorm:"not null"`
	UpdatedAt    time.Time `gorm:"not null"`
}

func (Admin) TableName() string {
	return "admins"
}
```

- [ ] **Step 4：驗證編譯與 migration 檔被辨識**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go build ./...`
Expected: 無錯誤。（migration 於 server 啟動時自動套用，Task 6 端到端驗證。）

- [ ] **Step 5：Commit**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add db/migrations/000005_create_admins.up.sql db/migrations/000005_create_admins.down.sql internal/model/models.go
git commit -m "feat(admin): admins 表 migration 與 Admin model

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2：AdminRepository

**Files:**
- Modify: `internal/repository/repository.go`（新增 `AdminRepository`）
- Test: `internal/repository/admin_repository_integration_test.go`

**Interfaces:**
- Consumes: `model.Admin`
- Produces:
  - `type AdminRepository struct{...}`、`func NewAdminRepository(db *gorm.DB) *AdminRepository`
  - `func (r *AdminRepository) FindByEmail(email string) (*model.Admin, error)`
  - `func (r *AdminRepository) Create(a *model.Admin) error`
  - `func (r *AdminRepository) CountAll() (int64, error)`

- [ ] **Step 0：新增「跑真 migration」的測試 helper**

⚠️ 既有 `newTestDB(t)`（geofence_integration_test.go）只 raw SQL 建「最小 rides 表」，**沒有 admins/customers/drivers**。故新增一個跑真 migration 的 helper（容器的 ConnectionString 就是 `postgres://...` migrate URL 格式，直接餵給既有 `database.RunMigrations`）。

Create `internal/repository/migrated_db_test.go`：
```go
package repository

import (
	"context"
	"testing"

	tcpostgres "github.com/testcontainers/testcontainers-go/modules/postgres"
	gormpostgres "gorm.io/driver/postgres"
	"gorm.io/gorm"

	"line-fleet-dispatch/internal/database"
)

// newMigratedTestDB 起真 PostGIS 容器並跑「全部 db/migrations」，得到與正式一致的完整 schema。
// Docker 不可用時跳過。
func newMigratedTestDB(t *testing.T) *gorm.DB {
	t.Helper()
	ctx := context.Background()
	container, err := tcpostgres.Run(ctx, "postgis/postgis:16-3.4",
		tcpostgres.WithDatabase("test"),
		tcpostgres.WithUsername("test"),
		tcpostgres.WithPassword("test"),
		tcpostgres.BasicWaitStrategies(),
	)
	if err != nil {
		t.Skipf("略過整合測試（Docker/testcontainers 不可用）: %v", err)
	}
	t.Cleanup(func() { _ = container.Terminate(ctx) })

	connStr, err := container.ConnectionString(ctx, "sslmode=disable")
	if err != nil {
		t.Fatalf("取得連線字串失敗: %v", err)
	}
	// connStr 為 postgres://test:test@host:port/test?sslmode=disable，正是 migrate URL 格式
	if err := database.RunMigrations(connStr, "../../db/migrations"); err != nil {
		t.Fatalf("跑 migration 失敗: %v", err)
	}
	db, err := gorm.Open(gormpostgres.Open(connStr), &gorm.Config{})
	if err != nil {
		t.Fatalf("連線失敗: %v", err)
	}
	return db
}
```

> 前置確認：`database.RunMigrations(url, path string) error` 的簽名——先讀 `internal/database/database.go` 確認參數順序與名稱；若不同，據實調整這裡的呼叫。

- [ ] **Step 1：寫失敗整合測試**

Create `internal/repository/admin_repository_integration_test.go`：
```go
package repository

import (
	"testing"
	"time"

	"line-fleet-dispatch/internal/model"
)

func TestAdminRepository_建立與查詢(t *testing.T) {
	db := newMigratedTestDB(t)
	repo := NewAdminRepository(db)

	n0, err := repo.CountAll()
	if err != nil {
		t.Fatalf("CountAll 失敗: %v", err)
	}

	now := time.Now()
	a := &model.Admin{Email: "ops@example.com", PasswordHash: "hash", Name: "值班", CreatedAt: now, UpdatedAt: now}
	if err := repo.Create(a); err != nil {
		t.Fatalf("Create 失敗: %v", err)
	}
	if a.ID == 0 {
		t.Fatal("Create 後未回填 ID")
	}

	got, err := repo.FindByEmail("ops@example.com")
	if err != nil {
		t.Fatalf("FindByEmail 失敗: %v", err)
	}
	if got.Email != "ops@example.com" || got.Name != "值班" {
		t.Fatalf("查詢結果錯誤: %+v", got)
	}

	n1, err := repo.CountAll()
	if err != nil {
		t.Fatalf("CountAll 失敗: %v", err)
	}
	if n1 != n0+1 {
		t.Fatalf("CountAll 預期 %d，得到 %d", n0+1, n1)
	}
}
```

> 前置確認：`newTestDB(t)` 是否存在於 `internal/repository` 既有整合測試。若函式名不同，**先讀 `internal/repository/geofence_integration_test.go` 找出建立測試 DB 的 helper 名稱並改用之**（migration 會建 admins 表）。

- [ ] **Step 2：跑測試確認失敗**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/repository/ -run TestAdminRepository -v`
Expected: FAIL（`undefined: NewAdminRepository`）。

- [ ] **Step 3：實作**

在 `internal/repository/repository.go` 末尾新增：
```go
type AdminRepository struct {
	db *gorm.DB
}

func NewAdminRepository(db *gorm.DB) *AdminRepository {
	return &AdminRepository{db: db}
}

func (r *AdminRepository) FindByEmail(email string) (*model.Admin, error) {
	var a model.Admin
	if err := r.db.Where("email = ?", email).First(&a).Error; err != nil {
		return nil, err
	}
	return &a, nil
}

func (r *AdminRepository) Create(a *model.Admin) error {
	return r.db.Create(a).Error
}

func (r *AdminRepository) CountAll() (int64, error) {
	var n int64
	err := r.db.Model(&model.Admin{}).Count(&n).Error
	return n, err
}
```

- [ ] **Step 4：跑測試確認通過**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/repository/ -run TestAdminRepository -v`
Expected: PASS（需 Docker）。

- [ ] **Step 5：Commit**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add internal/repository/repository.go internal/repository/admin_repository_integration_test.go
git commit -m "feat(admin): AdminRepository（FindByEmail/Create/CountAll）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3：AdminRegistry 服務（註冊/登入/種子）

**Files:**
- Create: `internal/service/admin_registry.go`
- Test: `internal/service/admin_registry_test.go`

**Interfaces:**
- Consumes: `repository.AdminRepository`、`model.Admin`、既有 `ErrInvalidCredentials`
- Produces:
  - `type AdminRegistry struct{...}`、`func NewAdminRegistry(admins *repository.AdminRepository) *AdminRegistry`
  - `func (s *AdminRegistry) Login(ctx context.Context, email, password string) (*model.Admin, error)`
  - `func (s *AdminRegistry) EnsureSeed(ctx context.Context, email, password string) error`（無任何 admin 且提供了 email/password 時建立一個）

**設計要點：** 鏡射 `DriverRegistry`。`EnsureSeed` 供啟動時用環境變數種一個管理員，避免開放公開註冊端點（安全）。

- [ ] **Step 1：寫失敗測試（用假 repo，純邏輯，不需 DB）**

Create `internal/service/admin_registry_test.go`：
```go
package service

import (
	"context"
	"testing"
	"time"

	"golang.org/x/crypto/bcrypt"

	"line-fleet-dispatch/internal/model"
)

// fakeAdminStore 以記憶體模擬 AdminRepository 的行為（僅測試 AdminRegistry 邏輯）
type fakeAdminStore struct {
	byEmail map[string]*model.Admin
	nextID  int64
}

func newFakeAdminStore() *fakeAdminStore {
	return &fakeAdminStore{byEmail: map[string]*model.Admin{}, nextID: 1}
}
func (f *fakeAdminStore) FindByEmail(email string) (*model.Admin, error) {
	a, ok := f.byEmail[email]
	if !ok {
		return nil, errNotFoundFake
	}
	return a, nil
}
func (f *fakeAdminStore) Create(a *model.Admin) error {
	a.ID = f.nextID
	f.nextID++
	f.byEmail[a.Email] = a
	return nil
}
func (f *fakeAdminStore) CountAll() (int64, error) { return int64(len(f.byEmail)), nil }

func TestAdminRegistry_種子與登入(t *testing.T) {
	store := newFakeAdminStore()
	reg := NewAdminRegistry(store)
	ctx := context.Background()

	// 種子建立一個管理員
	if err := reg.EnsureSeed(ctx, "ops@example.com", "s3cret"); err != nil {
		t.Fatalf("EnsureSeed 失敗: %v", err)
	}
	// 再次種子不應重複建立
	if err := reg.EnsureSeed(ctx, "ops@example.com", "s3cret"); err != nil {
		t.Fatalf("重複 EnsureSeed 失敗: %v", err)
	}
	if n, _ := store.CountAll(); n != 1 {
		t.Fatalf("種子應只建立 1 個管理員，實際 %d", n)
	}

	// 正確密碼可登入
	admin, err := reg.Login(ctx, "ops@example.com", "s3cret")
	if err != nil {
		t.Fatalf("登入失敗: %v", err)
	}
	if admin.Email != "ops@example.com" {
		t.Fatalf("登入回傳錯誤: %+v", admin)
	}
	// 驗證密碼確實是 bcrypt 雜湊
	if bcrypt.CompareHashAndPassword([]byte(admin.PasswordHash), []byte("s3cret")) != nil {
		t.Fatal("密碼未正確以 bcrypt 儲存")
	}

	// 錯誤密碼被拒
	if _, err := reg.Login(ctx, "ops@example.com", "wrong"); err == nil {
		t.Fatal("錯誤密碼應登入失敗")
	}
	_ = time.Now
}
```

> 註：`errNotFoundFake` 於 Step 3 一併定義在測試檔外的實作端不需要——它只用於測試，請在此測試檔頂部加 `var errNotFoundFake = errorsNew("not found")`。為免額外 import，改用既有 `ErrNotFound`（service 套件內已定義於 errors.go）。**將測試中 `errNotFoundFake` 全部改為 `ErrNotFound`**。

- [ ] **Step 2：跑測試確認失敗**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/service/ -run TestAdminRegistry -v`
Expected: FAIL（`undefined: NewAdminRegistry`）。

- [ ] **Step 3：實作**

Create `internal/service/admin_registry.go`：
```go
package service

import (
	"context"
	"errors"
	"time"

	"golang.org/x/crypto/bcrypt"
	"gorm.io/gorm"

	"line-fleet-dispatch/internal/model"
)

// adminStore 抽象出 AdminRegistry 需要的 repository 行為（方便測試替身）
type adminStore interface {
	FindByEmail(email string) (*model.Admin, error)
	Create(a *model.Admin) error
	CountAll() (int64, error)
}

// AdminRegistry 後台管理員登入與種子建立
type AdminRegistry struct {
	admins adminStore
}

func NewAdminRegistry(admins adminStore) *AdminRegistry {
	return &AdminRegistry{admins: admins}
}

// Login email + 密碼驗證
func (s *AdminRegistry) Login(ctx context.Context, email, password string) (*model.Admin, error) {
	admin, err := s.admins.FindByEmail(email)
	if err != nil {
		return nil, ErrInvalidCredentials
	}
	if admin.PasswordHash == "" ||
		bcrypt.CompareHashAndPassword([]byte(admin.PasswordHash), []byte(password)) != nil {
		return nil, ErrInvalidCredentials
	}
	return admin, nil
}

// EnsureSeed 若系統尚無任何管理員且提供了 email/password，建立一個種子管理員
func (s *AdminRegistry) EnsureSeed(ctx context.Context, email, password string) error {
	if email == "" || password == "" {
		return nil
	}
	n, err := s.admins.CountAll()
	if err != nil {
		return err
	}
	if n > 0 {
		return nil
	}
	hash, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)
	if err != nil {
		return err
	}
	now := time.Now()
	return s.admins.Create(&model.Admin{
		Email:        email,
		PasswordHash: string(hash),
		Name:         "系統管理員",
		CreatedAt:    now,
		UpdatedAt:    now,
	})
}

// 讓 *repository.AdminRepository 的 FindByEmail 回傳的 gorm not found 也能被視為登入失敗
var _ = errors.Is
var _ = gorm.ErrRecordNotFound
```

> 註：最後兩行 `var _ =` 僅為說明相容性，實作上 `Login` 已把任何 `FindByEmail` 錯誤轉成 `ErrInvalidCredentials`，故**可省略那兩行與對應 import**（`errors`、`gorm`）。實作時若不需要就不要 import，保持乾淨。

- [ ] **Step 4：跑測試確認通過**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/service/ -run TestAdminRegistry -v`
Expected: PASS。

- [ ] **Step 5：Commit**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add internal/service/admin_registry.go internal/service/admin_registry_test.go
git commit -m "feat(admin): AdminRegistry 登入與環境變數種子

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4：AdminAuth 中介層

**Files:**
- Modify: `internal/middleware/auth.go`（新增 `AdminAuth`、`AdminIDFromCtx`）
- Test: `internal/middleware/admin_auth_test.go`

**Interfaces:**
- Consumes: `auth.ParseToken`（既有）、`events.RoleAdmin`
- Produces:
  - `const CtxAdminID = "admin_id"`
  - `func AdminAuth(secret string) gin.HandlerFunc`（驗 Bearer JWT 且 role=="admin"，否則 401/403）
  - `func AdminIDFromCtx(c *gin.Context) int64`

- [ ] **Step 1：寫失敗測試**

Create `internal/middleware/admin_auth_test.go`：
```go
package middleware

import (
	"net/http"
	"net/http/httptest"
	"testing"
	"time"

	"github.com/gin-gonic/gin"

	"line-fleet-dispatch/internal/auth"
)

func setupAdminRouter(secret string) *gin.Engine {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.GET("/admin/ping", AdminAuth(secret), func(c *gin.Context) {
		c.JSON(http.StatusOK, gin.H{"admin_id": AdminIDFromCtx(c)})
	})
	return r
}

func TestAdminAuth_合法admin通過(t *testing.T) {
	secret := "s"
	tok, _ := auth.GenerateToken("admin", 5, secret, time.Hour)
	r := setupAdminRouter(secret)
	w := httptest.NewRecorder()
	req, _ := http.NewRequest("GET", "/admin/ping", nil)
	req.Header.Set("Authorization", "Bearer "+tok)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusOK {
		t.Fatalf("預期 200，得到 %d", w.Code)
	}
}

func TestAdminAuth_無token回401(t *testing.T) {
	r := setupAdminRouter("s")
	w := httptest.NewRecorder()
	req, _ := http.NewRequest("GET", "/admin/ping", nil)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusUnauthorized {
		t.Fatalf("預期 401，得到 %d", w.Code)
	}
}

func TestAdminAuth_司機token被拒403(t *testing.T) {
	secret := "s"
	tok, _ := auth.GenerateToken("driver", 9, secret, time.Hour)
	r := setupAdminRouter(secret)
	w := httptest.NewRecorder()
	req, _ := http.NewRequest("GET", "/admin/ping", nil)
	req.Header.Set("Authorization", "Bearer "+tok)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusForbidden {
		t.Fatalf("預期 403，得到 %d", w.Code)
	}
}
```

- [ ] **Step 2：跑測試確認失敗**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/middleware/ -run TestAdminAuth -v`
Expected: FAIL（`undefined: AdminAuth`）。

- [ ] **Step 3：實作**

在 `internal/middleware/auth.go` 末尾新增：
```go
// CtxAdminID 存放經 JWT 驗證後的管理員 id
const CtxAdminID = "admin_id"

// AdminAuth 驗證 Bearer JWT 且角色為 admin；非 admin 回 403，無效 token 回 401
func AdminAuth(secret string) gin.HandlerFunc {
	return func(c *gin.Context) {
		header := c.GetHeader("Authorization")
		if !strings.HasPrefix(header, "Bearer ") {
			c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"error": "缺少或格式錯誤的授權標頭"})
			return
		}
		role, id, err := auth.ParseToken(strings.TrimPrefix(header, "Bearer "), secret)
		if err != nil {
			c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"error": "token 無效或已過期"})
			return
		}
		if role != "admin" {
			c.AbortWithStatusJSON(http.StatusForbidden, gin.H{"error": "需要管理員權限"})
			return
		}
		c.Set(CtxAdminID, id)
		c.Next()
	}
}

// AdminIDFromCtx 取出中介層放入的 admin_id
func AdminIDFromCtx(c *gin.Context) int64 {
	if v, ok := c.Get(CtxAdminID); ok {
		if id, ok := v.(int64); ok {
			return id
		}
	}
	return 0
}
```

- [ ] **Step 4：跑測試確認通過**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/middleware/ -v`
Expected: PASS（含既有簽章測試不回歸）。

- [ ] **Step 5：Commit**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add internal/middleware/auth.go internal/middleware/admin_auth_test.go
git commit -m "feat(admin): AdminAuth 中介層（role=admin，非 admin 回 403）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5：後台查詢方法（Redis 在線司機、司機列表、訂單列表）

**Files:**
- Modify: `internal/redis/store.go`（新增 `OnlineDriverLocations`）
- Modify: `internal/repository/repository.go`（新增 `DriverRepository.ListAll`、`RideRepository.ListRecent`）
- Test: `internal/redis/store_admin_integration_test.go`、`internal/repository/admin_query_integration_test.go`

**Interfaces:**
- Produces:
  - `type DriverLoc struct { DriverID int64 \`json:"driver_id"\`; Lat float64 \`json:"lat"\`; Lng float64 \`json:"lng"\`; UpdatedAt int64 \`json:"updated_at"\` }`
  - `func (s *Store) OnlineDriverLocations(ctx context.Context) ([]DriverLoc, error)`（讀 `drivers:geo` 全體，過濾離線）
  - `func (r *DriverRepository) ListAll() ([]model.Driver, error)`
  - `type AdminRideRow struct{...}` + `func (r *RideRepository) ListRecent(status *int16, limit int) ([]AdminRideRow, error)`

- [ ] **Step 1：寫失敗測試（Redis）**

Create `internal/redis/store_admin_integration_test.go`：
```go
package redisstore

import (
	"context"
	"testing"
)

// newTestStore(t, offlineSec) 由既有整合測試提供（store_integration_test.go）。
func TestOnlineDriverLocations_列出在線司機(t *testing.T) {
	ctx := context.Background()
	store := newTestStore(t, 60)

	if err := store.UpdateDriverLocation(ctx, 101, 25.03, 121.56); err != nil {
		t.Fatalf("更新位置失敗: %v", err)
	}
	if err := store.UpdateDriverLocation(ctx, 102, 25.04, 121.57); err != nil {
		t.Fatalf("更新位置失敗: %v", err)
	}

	locs, err := store.OnlineDriverLocations(ctx)
	if err != nil {
		t.Fatalf("OnlineDriverLocations 失敗: %v", err)
	}
	if len(locs) < 2 {
		t.Fatalf("預期至少 2 位在線司機，得到 %d", len(locs))
	}
	found := map[int64]bool{}
	for _, l := range locs {
		found[l.DriverID] = true
		if l.Lat == 0 && l.Lng == 0 {
			t.Fatalf("司機 %d 座標為空", l.DriverID)
		}
	}
	if !found[101] || !found[102] {
		t.Fatalf("未列出預期司機: %+v", locs)
	}
}
```

> 前置確認：讀 `internal/redis/store_integration_test.go` 找出建立 `*Store` 的 helper（可能名為 `newTestStore` 或類似），改用其名稱。

- [ ] **Step 2：跑測試確認失敗**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/redis/ -run TestOnlineDriverLocations -v`
Expected: FAIL（`undefined: OnlineDriverLocations`）。

- [ ] **Step 3：實作 Redis 方法**

在 `internal/redis/store.go` 末尾新增：
```go
// DriverLoc 後台車隊快照用：單一司機的即時座標
type DriverLoc struct {
	DriverID  int64   `json:"driver_id"`
	Lat       float64 `json:"lat"`
	Lng       float64 `json:"lng"`
	UpdatedAt int64   `json:"updated_at"`
}

// OnlineDriverLocations 列出所有「未離線」司機的即時座標（後台車隊快照）。
// 從 drivers:geo（zset）取全體成員，再以各司機 hash 的 updated_at 過濾離線。
func (s *Store) OnlineDriverLocations(ctx context.Context) ([]DriverLoc, error) {
	ids, err := s.client.ZRange(ctx, driversGeoKey, 0, -1).Result()
	if err != nil {
		return nil, err
	}
	cutoff := time.Now().Add(-time.Duration(s.driverOfflineSec) * time.Second).Unix()
	var out []DriverLoc
	for _, idStr := range ids {
		m, err := s.client.HGetAll(ctx, fmt.Sprintf("driver:%s:loc", idStr)).Result()
		if err != nil || len(m) == 0 {
			continue
		}
		updatedAt, _ := strconv.ParseInt(m["updated_at"], 10, 64)
		if updatedAt < cutoff {
			continue
		}
		id, err := strconv.ParseInt(idStr, 10, 64)
		if err != nil {
			continue
		}
		lat, _ := strconv.ParseFloat(m["lat"], 64)
		lng, _ := strconv.ParseFloat(m["lng"], 64)
		out = append(out, DriverLoc{DriverID: id, Lat: lat, Lng: lng, UpdatedAt: updatedAt})
	}
	return out, nil
}
```

- [ ] **Step 4：跑 Redis 測試確認通過**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/redis/ -run TestOnlineDriverLocations -v`
Expected: PASS（需 Docker）。

- [ ] **Step 5：寫失敗測試（訂單列表 repo）**

Create `internal/repository/admin_query_integration_test.go`：
```go
package repository

import (
	"testing"
	"time"

	"line-fleet-dispatch/internal/constants"
	"line-fleet-dispatch/internal/model"
)

func TestRideListRecent_依狀態篩選(t *testing.T) {
	db := newMigratedTestDB(t)
	rideRepo := NewRideRepository(db)
	custRepo := NewCustomerRepository(db)

	cust, err := custRepo.FindOrCreateByLineUserID("U_admin_test", "測試客")
	if err != nil {
		t.Fatalf("建立客戶失敗: %v", err)
	}
	now := time.Now()
	ride := &model.Ride{
		CustomerID:    cust.ID,
		Status:        constants.RideStatusRequested,
		PickupPoint:   model.GeoPoint{Lat: 25.03, Lng: 121.56},
		PickupAddress: "台北車站",
		RequestedAt:   now,
		CreatedAt:     now,
		UpdatedAt:     now,
	}
	if err := rideRepo.Create(ride); err != nil {
		t.Fatalf("建立訂單失敗: %v", err)
	}

	rows, err := rideRepo.ListRecent(nil, 50)
	if err != nil {
		t.Fatalf("ListRecent 失敗: %v", err)
	}
	if len(rows) == 0 {
		t.Fatal("ListRecent 應至少回一筆")
	}

	st := int16(constants.RideStatusRequested)
	filtered, err := rideRepo.ListRecent(&st, 50)
	if err != nil {
		t.Fatalf("ListRecent(status) 失敗: %v", err)
	}
	for _, r := range filtered {
		if r.Status != constants.RideStatusRequested {
			t.Fatalf("篩選後出現非 REQUESTED 狀態: %+v", r)
		}
	}
}

func TestDriverListAll(t *testing.T) {
	db := newMigratedTestDB(t)
	repo := NewDriverRepository(db)
	if _, err := repo.CreateSimulated("後台測試司機", "U_admin_drv"); err != nil {
		t.Fatalf("建立司機失敗: %v", err)
	}
	all, err := repo.ListAll()
	if err != nil {
		t.Fatalf("ListAll 失敗: %v", err)
	}
	if len(all) == 0 {
		t.Fatal("ListAll 應至少回一筆")
	}
}
```

- [ ] **Step 6：跑測試確認失敗**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/repository/ -run 'TestRideListRecent|TestDriverListAll' -v`
Expected: FAIL（`undefined: ListRecent` / `ListAll`）。

- [ ] **Step 7：實作 repo 方法**

在 `internal/repository/repository.go` 末尾新增：
```go
func (r *DriverRepository) ListAll() ([]model.Driver, error) {
	var drivers []model.Driver
	err := r.db.Order("id ASC").Find(&drivers).Error
	return drivers, err
}

// AdminRideRow 後台訂單列表的投影（不含地理欄位，避免 GeoPoint 掃描問題）
type AdminRideRow struct {
	ID            int64      `json:"id"`
	CustomerID    int64      `json:"customer_id"`
	DriverID      *int64     `json:"driver_id"`
	Status        int16      `json:"status"`
	PickupAddress string     `json:"pickup_address"`
	RequestedAt   time.Time  `json:"requested_at"`
	CompletedAt   *time.Time `json:"completed_at"`
	DistanceM     *int       `json:"distance_m"`
}

// ListRecent 後台訂單列表：可選 status 篩選，依 id 由新到舊取 limit 筆
func (r *RideRepository) ListRecent(status *int16, limit int) ([]AdminRideRow, error) {
	if limit <= 0 || limit > 500 {
		limit = 100
	}
	q := r.db.Model(&model.Ride{}).
		Select("id", "customer_id", "driver_id", "status", "pickup_address", "requested_at", "completed_at", "distance_m").
		Order("id DESC").Limit(limit)
	if status != nil {
		q = q.Where("status = ?", *status)
	}
	var rows []AdminRideRow
	err := q.Scan(&rows).Error
	return rows, err
}
```

- [ ] **Step 8：跑測試確認通過**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/repository/ -run 'TestRideListRecent|TestDriverListAll' -v`
Expected: PASS。

- [ ] **Step 9：Commit**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add internal/redis/store.go internal/redis/store_admin_integration_test.go internal/repository/repository.go internal/repository/admin_query_integration_test.go
git commit -m "feat(admin): 後台查詢方法（在線司機快照/司機列表/訂單列表）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6：AdminHandler 與路由接線（含啟動種子）

**Files:**
- Create: `internal/handler/admin.go`
- Modify: `internal/config/config.go`（新增 `AdminSeedEmail`、`AdminSeedPassword`）
- Modify: `cmd/server/main.go`（建立 repo/service/handler、種子、註冊 `/api/admin/*` 路由）
- Test: `internal/handler/admin_test.go`

**Interfaces:**
- Consumes: `service.AdminRegistry`、`repository.*`、`redisstore.Store`、`middleware.AdminAuth`、`auth.GenerateToken`
- Produces:
  - `type AdminHandler struct{...}` + `func NewAdminHandler(...) *AdminHandler`
  - handler 方法：`Login`、`Fleet`、`Drivers`、`Rides`、`RideDetail`、`DailyReport`

- [ ] **Step 1：新增設定欄位**

在 `internal/config/config.go` 的 `Config` struct（WS 欄位之後）加：
```go
	AdminSeedEmail    string
	AdminSeedPassword string
```
在 `Load()` 加：
```go
		AdminSeedEmail:    getEnv("ADMIN_SEED_EMAIL", ""),
		AdminSeedPassword: getEnv("ADMIN_SEED_PASSWORD", ""),
```

- [ ] **Step 2：寫失敗測試（login 用假 registry；授權邊界）**

Create `internal/handler/admin_test.go`：
```go
package handler

import (
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
	"time"

	"github.com/gin-gonic/gin"

	"line-fleet-dispatch/internal/auth"
	"line-fleet-dispatch/internal/middleware"
)

func TestAdminFleet_未授權回401(t *testing.T) {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	// 只掛需要 admin 的路由，不注入真服務（驗證授權先於 handler 執行）
	r.GET("/api/admin/fleet", middleware.AdminAuth("s"), func(c *gin.Context) {
		c.JSON(http.StatusOK, gin.H{"ok": true})
	})

	w := httptest.NewRecorder()
	req, _ := http.NewRequest("GET", "/api/admin/fleet", nil)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusUnauthorized {
		t.Fatalf("預期 401，得到 %d", w.Code)
	}

	// admin token 可過授權
	tok, _ := auth.GenerateToken("admin", 1, "s", time.Hour)
	w2 := httptest.NewRecorder()
	req2, _ := http.NewRequest("GET", "/api/admin/fleet", nil)
	req2.Header.Set("Authorization", "Bearer "+tok)
	r.ServeHTTP(w2, req2)
	if w2.Code != http.StatusOK {
		t.Fatalf("admin 應通過授權，得到 %d", w2.Code)
	}
	_ = strings.TrimSpace
}
```

> 說明：AdminHandler 各端點的資料正確性由 Task 5 的 repo/redis 整合測試涵蓋；此 handler 測試聚焦「授權邊界」——handler 本體是薄接線，避免在此重複起 DB。

- [ ] **Step 3：跑測試確認失敗**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./internal/handler/ -run TestAdminFleet -v`
Expected: 一開始可能 PASS（因為測試未用到 AdminHandler 型別）。**若 PASS 則補一行編譯依賴**：在測試頂部加 `var _ = NewAdminHandler` 使其依賴 handler 型別、確保 Task 步驟推動實作。跑後應 FAIL（`undefined: NewAdminHandler`）。

- [ ] **Step 4：實作 AdminHandler**

Create `internal/handler/admin.go`：
```go
package handler

import (
	"net/http"
	"strconv"
	"time"

	"github.com/gin-gonic/gin"

	"line-fleet-dispatch/internal/auth"
	"line-fleet-dispatch/internal/repository"
	redisstore "line-fleet-dispatch/internal/redis"
	"line-fleet-dispatch/internal/service"
)

// AdminHandler 後台唯讀 API + 登入
type AdminHandler struct {
	admins         *service.AdminRegistry
	drivers        *repository.DriverRepository
	rides          *repository.RideRepository
	tracks         *repository.TrackRepository
	reports        *repository.ReportRepository
	redis          *redisstore.Store
	jwtSecret      string
	jwtExpiryHours int
}

func NewAdminHandler(
	admins *service.AdminRegistry,
	drivers *repository.DriverRepository,
	rides *repository.RideRepository,
	tracks *repository.TrackRepository,
	reports *repository.ReportRepository,
	redis *redisstore.Store,
	jwtSecret string,
	jwtExpiryHours int,
) *AdminHandler {
	return &AdminHandler{
		admins: admins, drivers: drivers, rides: rides, tracks: tracks,
		reports: reports, redis: redis, jwtSecret: jwtSecret, jwtExpiryHours: jwtExpiryHours,
	}
}

// Login POST /api/admin/login
func (h *AdminHandler) Login(c *gin.Context) {
	var req struct {
		Email    string `json:"email" binding:"required"`
		Password string `json:"password" binding:"required"`
	}
	if err := c.ShouldBindJSON(&req); err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"error": "參數錯誤"})
		return
	}
	admin, err := h.admins.Login(c.Request.Context(), req.Email, req.Password)
	if err != nil {
		c.JSON(http.StatusUnauthorized, gin.H{"error": err.Error()})
		return
	}
	token, err := auth.GenerateToken("admin", admin.ID, h.jwtSecret, time.Duration(h.jwtExpiryHours)*time.Hour)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": "簽發 token 失敗"})
		return
	}
	c.JSON(http.StatusOK, gin.H{"admin_id": admin.ID, "name": admin.Name, "token": token})
}

// Fleet GET /api/admin/fleet：即時在線司機座標快照
func (h *AdminHandler) Fleet(c *gin.Context) {
	locs, err := h.redis.OnlineDriverLocations(c.Request.Context())
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{"drivers": locs})
}

// Drivers GET /api/admin/drivers：司機列表
func (h *AdminHandler) Drivers(c *gin.Context) {
	drivers, err := h.drivers.ListAll()
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{"drivers": drivers})
}

// Rides GET /api/admin/rides?status=0&limit=100：訂單列表
func (h *AdminHandler) Rides(c *gin.Context) {
	var statusPtr *int16
	if s := c.Query("status"); s != "" {
		if n, err := strconv.ParseInt(s, 10, 16); err == nil {
			v := int16(n)
			statusPtr = &v
		}
	}
	limit := 100
	if l := c.Query("limit"); l != "" {
		if n, err := strconv.Atoi(l); err == nil {
			limit = n
		}
	}
	rows, err := h.rides.ListRecent(statusPtr, limit)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{"rides": rows})
}

// RideDetail GET /api/admin/rides/:id：單筆訂單 + 軌跡 GeoJSON
func (h *AdminHandler) RideDetail(c *gin.Context) {
	id, err := strconv.ParseInt(c.Param("id"), 10, 64)
	if err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"error": "id 格式錯誤"})
		return
	}
	ride, err := h.rides.GetByID(id)
	if err != nil {
		c.JSON(http.StatusNotFound, gin.H{"error": "找不到訂單"})
		return
	}
	geojson, err := h.tracks.GeoJSON(id)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{"ride": ride, "track_geojson": geojson})
}

// DailyReport GET /api/admin/reports/daily?date=2026-07-06
func (h *AdminHandler) DailyReport(c *gin.Context) {
	date := c.Query("date")
	if date == "" {
		c.JSON(http.StatusBadRequest, gin.H{"error": "請提供 date 參數"})
		return
	}
	rows, err := h.reports.DailyDriverStats(date)
	if err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": err.Error()})
		return
	}
	c.JSON(http.StatusOK, gin.H{"date": date, "drivers": rows})
}
```

- [ ] **Step 5：接線 main.go（種子 + 路由）**

在 `cmd/server/main.go`：

5a. 建立 admin repo/service/handler（在其他 repo/handler 建立處附近）：
```go
	adminRepo := repository.NewAdminRepository(db)
	adminRegistry := service.NewAdminRegistry(adminRepo)
	if err := adminRegistry.EnsureSeed(context.Background(), cfg.AdminSeedEmail, cfg.AdminSeedPassword); err != nil {
		log.Error().Err(err).Msg("建立種子管理員失敗")
	}
	adminHandler := handler.NewAdminHandler(
		adminRegistry, driverRepo, rideRepo, trackRepo, reportRepo, redisStore,
		cfg.JWTSecret, cfg.JWTExpiryHours,
	)
```
（`context` 已於 main.go import；`log` 亦已 import。）

5b. 在 `api := r.Group("/api")` 區塊內新增 admin 路由：
```go
		// 後台：登入公開，其餘受 admin JWT 保護
		api.POST("/admin/login", adminHandler.Login)
		adminG := api.Group("/admin")
		adminG.Use(middleware.AdminAuth(cfg.JWTSecret))
		{
			adminG.GET("/fleet", adminHandler.Fleet)
			adminG.GET("/drivers", adminHandler.Drivers)
			adminG.GET("/rides", adminHandler.Rides)
			adminG.GET("/rides/:id", adminHandler.RideDetail)
			adminG.GET("/reports/daily", adminHandler.DailyReport)
		}
```

- [ ] **Step 6：跑 handler 測試 + 全量編譯/測試**

Run:
```bash
cd /Users/mac/Documents/line-fleet-dispatch
CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go vet ./... && CGO_ENABLED=0 go test ./internal/handler/ ./internal/middleware/ ./internal/service/ -v 2>&1 | tail -30
```
Expected: 編譯、vet、測試皆綠。

- [ ] **Step 7：Commit**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add internal/handler/admin.go internal/config/config.go cmd/server/main.go
git commit -m "feat(admin): AdminHandler 唯讀 API 與 /api/admin 路由（含啟動種子）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7：更新 .env.example、決策紀錄，全量測試與 push

**Files:**
- Modify: `.env.example`
- Modify: `docs/decisions.md`

- [ ] **Step 1：補 .env.example**

在 `.env.example` 末尾加：
```dotenv
# 後台管理員種子（首次啟動且尚無任何 admin 時建立；建立後可清空）
ADMIN_SEED_EMAIL=
ADMIN_SEED_PASSWORD=
```

- [ ] **Step 2：補決策紀錄**

在 `docs/decisions.md` 末尾加：
```markdown
## 2026-07-06 · M5-ADMIN 後台認證與唯讀 API

- **admin 認證**：新 `admins` 表 + bcrypt 密碼，沿用通用角色 JWT（role=admin）。
- **不開放公開註冊**：管理員由啟動時環境變數 `ADMIN_SEED_EMAIL/PASSWORD` 種子建立，
  且僅在系統尚無任何 admin 時建立一次。
- **唯讀優先**：本階段只做讀取端點（車隊/司機/訂單/軌跡/報表）。司機停用等寫入操作
  刻意延後——真正的停用需派單邏輯配合，否則是假功能。
- **車隊快照**：`OnlineDriverLocations` 讀 `drivers:geo` zset 全體再依 updated_at 過濾離線；
  即時更新由已完成的 WS `driver.location`（admin 廣播）承擔。
```

- [ ] **Step 3：全量測試（含整合）**

Run: `cd /Users/mac/Documents/line-fleet-dispatch && CGO_ENABLED=0 go test ./... 2>&1 | tail -20`
Expected: 全綠（含 testcontainers 整合測試；需 Docker）。

- [ ] **Step 4：Commit 與 push**

```bash
cd /Users/mac/Documents/line-fleet-dispatch
git add .env.example docs/decisions.md
git commit -m "docs(admin): 補 admin 種子環境變數與決策紀錄

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git push origin main
```

---

## Self-Review

**1. Spec coverage（對照設計 §5.4 後台 API）**
- `/api/admin/fleet` 即時在線司機 → Task 5+6 ✅
- `/api/admin/drivers` 司機列表 → Task 5+6 ✅（審核啟用/停用 = 寫入，**本 plan 明確延後**，Global Constraints 已聲明）
- `/api/admin/rides` 訂單列表 + 詳情 + 軌跡 GeoJSON → Task 5+6（Rides/RideDetail）✅
- `/api/admin/reports/daily` 日報表 → Task 6 ✅
- `/api/admin/settings` 派單參數（寫入）→ **延後**（同司機停用，屬寫入類）
- admin 認證（§5.1）→ Task 1~4 ✅

**2. Placeholder scan：** 無 TBD/TODO。Task 3 的兩處 `var _ =` 已明確標示「可省略」並附條件；Task 2/5 的 `newTestDB`/`newTestStore` 前置確認步驟要求先讀既有整合測試檔確認 helper 名稱——非佔位符，是防呆。

**3. Type consistency：**
- `auth.GenerateToken(role, id, secret, ttl)` / `ParseToken`（M5-WS 已實作）於 Task 4/6 使用一致 ✅
- `adminStore` 介面（Task 3）方法 `FindByEmail/Create/CountAll` 與 `AdminRepository`（Task 2）方法簽名一致——`*repository.AdminRepository` 可直接滿足 `adminStore` ✅
- `redisstore.DriverLoc`、`OnlineDriverLocations`（Task 5）與 handler `Fleet`（Task 6）一致 ✅
- `AdminRideRow`、`ListRecent(*int16, int)`（Task 5）與 handler `Rides`（Task 6）一致 ✅

**4. 依賴順序：** Task 1→2→3→4→5 可平行推進；Task 6 依賴 2/3/4/5；Task 7 最後。順序正確。

**5. 前置確認事項（執行者務必做）：**
- Task 2/5：`internal/repository` 與 `internal/redis` 既有整合測試建立 DB/Store 的 helper 實際名稱——先讀既有 `*_integration_test.go` 確認，勿假設 `newTestDB`/`newTestStore` 就是對的名字。
